#ifndef PARSER_H
#define PARSER_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

//takes a character string pointer and parses it into a character array pointer
char **parser(char *);

#endif
